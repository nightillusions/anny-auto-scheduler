{
  "name": "anny-auto-scheduler",
  "version": "1.0.0",
  "private": true,
  "description": "Microsoft-Edge-Erweiterung für wiederkehrende Anny-Reservierungen",
  "scripts": {
    "check": "node --check src/bridge-main.js && node --check src/content.js && node --check src/recurrence.js",
    "test": "node --test",
    "package": "node scripts/package.mjs"
  },
  "engines": { "node": ">=22" }
}
